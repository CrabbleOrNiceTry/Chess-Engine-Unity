using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class AI : MonoBehaviour
{
    // Start is called before the first frame update
    // public string IEnumerator GetMove(Square[] board, int depth, int initialDepth, bool player, float alpha, float beta)
    // {   
    //     if (depth == 0)
    // }
}
